#!/usr/bin/env node

/**
 * Command line interface of the-script-jsdoc.
 * This file is auto generated by ape-tmpl.
 */

'use strict'

const program = require('commander')
const pkg = require('../package')
const theScriptJsdoc = require('../lib')

program
  .version(pkg[ 'version' ])
  .usage('[options] <dirname> ')
  .description(pkg[ 'description' ])
program.parse(process.argv)

// Run main command
theScriptJsdoc.apply(theScriptJsdoc, program.args.concat({
})
).catch(handleError)

// Handlers

/** Handle error */
function handleError (err) {
  console.error(err)
  process.exit(1)
}
