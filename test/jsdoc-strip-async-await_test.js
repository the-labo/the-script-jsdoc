/**
 * Test case for jsdocStripAsyncAwait.
 * Runs with mocha.
 */
'use strict'

const jsdocStripAsyncAwait = require('../lib/plugins/jsdoc-strip-async-await.js')
const assert = require('assert')
const co = require('co')

describe('jsdoc-strip-async-await', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Jsdoc strip async await', () => co(function * () {

  }))
})

/* global describe, before, after, it */
